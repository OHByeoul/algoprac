1.
SELECT 
    ANIMAL_TYPE,
    COUNT(1)
FROM
    ANIMAL_INS
GROUP BY ANIMAL_TYPE
ORDER BY ANIMAL_TYPE

---------
2. 
SELECT
    NAME,
    COUNT(NAME)
FROM
    ANIMAL_INS 
GROUP BY NAME
HAVING COUNT(NAME) > 1
ORDER BY NAME;