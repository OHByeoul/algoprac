역순정렬

-- 코드를 입력하세요
SELECT 
    NAME,
    DATETIME
FROM
   ANIMAL_INS 
ORDER BY ANIMAL_ID DESC;

아픈 동물

SELECT
    ANIMAL_ID,
    NAME
FROM ANIMAL_INS
WHERE INTAKE_CONDITION = 'Sick'
ORDER BY ANIMAL_ID;    
------
어린 동물

SELECT
    ANIMAL_ID,
    NAME
FROM
    ANIMAL_INS
WHERE INTAKE_CONDITION != 'Aged'
ORDER BY ANIMAL_ID;